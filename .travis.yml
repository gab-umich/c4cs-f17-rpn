  language: python
  python:
    - 3.5
  install:
    - pip install coverage
    - pip install coveralls
  script:
    - python -m unittest test_rpn
    - coverage run -m unittest test_rpn
  after_success:
    - coveralls
